# Hand-written stubs for the slice of xero-python (untyped, no py.typed) that
# apps/xero consumes. Grown per-slice: stub what we import, no more. Signatures
# transcribed from the installed SDK (pinned xero-python==15.0.0), so a version
# bump must re-verify them.
