# Required — settings.py fails fast if any of these are missing.
JWT_SIGNING_KEY=
SECRET_KEY=
DEBUG=true
APP_DOMAIN=localhost
DB_NAME=docketworks_v2
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
REDIS_URL=redis://localhost:6379/1
FRONT_END_URL=http://localhost:5173
# Fable: the directory that DIRECTLY contains the Job-<number> folders —
# on a Dropbox-synced host the synced workflow subfolder, never the Dropbox
# root (one level too high 404s every attachment; 2026-08-31 production
# incident). Quote the value if the path contains a space.
DROPBOX_WORKFLOW_FOLDER=
PHONE_RECORDING_STORAGE_ROOT=
SESSION_REPLAY_STORAGE_ROOT=
MEDIA_ROOT=mediafiles
XERO_READONLY=false

# Optional — only the scrub-pipeline commands read it (`backport_data_backup`,
# the scrubbed-dump producer run on a production host, and
# `export_dev_demo_dump`). Must end in _scrub; leave empty everywhere else and
# no scrub database alias is defined.
SCRUB_DB_NAME=

# Optional — only config/settings_test.py reads these (never the app). Server
# instances get a per-tenant pytest role provisioned by
# scripts/server/instance.sh; dev checkouts leave both unset and pytest
# connects as DB_USER instead.
TEST_DB_USER=
TEST_DB_PASSWORD=

# Path to the Google service-account key JSON, and an override for the
# Workspace user it impersonates (defaults to CompanyDefaults.company_email).
# Read by apps/core/gauth.py, so the app needs it too: Gmail (password-reset
# email), the health-and-safety Drive import, the rclone backup upload and the
# scripts/gdocs/ authoring toolchain all fail loud without it. Optional only in
# a dev checkout that makes no Google calls.
GCP_CREDENTIALS=
GCP_DELEGATED_SUBJECT=
