# Copy to frontend/.env.test (gitignored). Playwright loads .env, then overlays
# this file (playwright.config.ts). Used by tests/e2e/fixtures/auth.ts.
E2E_TEST_USERNAME=
E2E_TEST_PASSWORD=

# Optional: point E2E at an already-running environment (e.g. your ngrok domain
# or http://localhost:4173). When set, Playwright skips starting its own preview
# server. Leave unset to let `npm run test:e2e` build + serve the preview itself.
# E2E_BASE_URL=http://localhost:4173
