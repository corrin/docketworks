#!/bin/bash
# Pre-push hook: runs frontend checks (mirrors old simple-git-hooks pre-push)
set -e

cd "$(git rev-parse --show-toplevel)/frontend"
npm run test:unit && npm run lint && npm run type-check
